console.log('TEST flat')

var results = [a, b, c]

var a = flat(notArray)
var b = flat(array1)
var c = flat(simpleArray)

results.forEach(function (result) {
    console.assert(result.length === )
    if (!(result instanceof Array)) {throw new TypeError(array + 'is not an array')}
})

var _error
try {
    forEach([1, 2, 3])
}
catch(error) {
    _error = error
}
finally {

}

function MyError (error) {
    error.message = 
}


