module.exports = (listinId, photoFileName) => {
    // TODO validate listing id and photo file name
    // TODO fs create read stream from path data/listings/<listingId>/<photoFileName> and return within the promise
}