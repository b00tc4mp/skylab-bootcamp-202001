module.exports = (userId, listingId, photoFileName) => {
    // TODO validate userId, listingId, photoFileName
    // TODO check user id and listing id are related
    // TODO fs remove file from data/<listingId>/<photoFileName>
}