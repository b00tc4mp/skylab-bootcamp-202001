import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center'
    },
    warning: {
        color: 'orange'
    },
    error: {
        backgroundColor: 'red'
    }
})

export default styles