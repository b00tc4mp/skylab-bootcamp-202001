const ht = "https" 
const app = "herokuapp"
const ipa = "/api"
const slash = "/"
const dot = "."
const script = "-"
const com = localStorage.getItem("company")

module.exports = {ht, app, ipa, slash, dot, script, com}